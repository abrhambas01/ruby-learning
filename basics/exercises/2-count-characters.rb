# returns the characters / number of characters in a string.

char = "abc12-"
puts "#{char}'s length is  #{char.length}"  # => 7

print "Enter a character:"
char = gets
print "#{char.length} is #{char} length"
